{{ define "nav" }}
    <div class="nav">
        {{if eq .name "homepage"}}
        <a href="/about">About</a> 
        {{else}}
        <a href="/">Home</a> 
        {{end}}
        <a href="mailto:{{.email}}">Contact</a>
        <a href="/projects">Projects</a>
        <a href="/writing">Writing</a>
    </div>
{{ end }}